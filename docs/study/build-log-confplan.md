# confplan build log and ground truth

Built 2026-08-09. This is the conference-planning testbed — one of two isomorphic study projects.

- **Repository:** `~/repos/sgt-study/confplan`
- **Starting point:** tag `study-start` at commit `4241cd0`
- **Size:** 26 commits, 38 passing tests
- **Author persona:** Sam Park <sam@conf.example.org>

The confplan project uses the same episode script as coursecraft (see `build-log-coursecraft.md`
for the episode shapes and subtask semantics). The nouns are swapped per the isomorphism table
in `docs/study/testbed-spec.md`.

## Key commits by study task

### S1 provenance (episode E8)
- `7ede859` — "add talk search": the search command plus a silent lowercase-day leniency fix in
  `slots.parse_slot`.

### S2/S3 waitlist chain (entangled removal and selective restore)
- `f85493e` + `1911a8f` — E11, split across two saves: logic first, then CLI wiring
- `d4051d6` — promotion
- `f8de0fd` — cancel cascade
- `b4e06f2` — notices
- `de52f62` — keeper fix (back-to-back)
- `4f9b974` — tangled keeper: agenda export + waitlist-hint fix

### E16 experiment (revert exemplar)
- `dfdcce6` — "speaker priority in session queues"
- `a8cd83f` — reverted cleanly by `sgt revert f-02c4a091@3`

Unlike coursecraft, this revert successfully deleted `tests/test_priority.py` from disk, so there
is no cleanup commit here.

### S4 regression (episode E17)
- `704e7a4` — introduces `ranges_clash` (boundary uses `<`)
- `6ca9a53` — routes registration through it (this second save is the one that changes behavior)
- The leftover `speaker` plumbing in models/cli is intentional — a realistic breadcrumb

## Known blemishes

- E0 is dated the build day (not the story day).
- E11 and E17 are each split into two same-day saves. These were edit mishaps during the build,
  kept because real histories have such pairs.
- Plan records for E2 and E11 over-claim slightly due to a resolver defect that was fixed
  mid-build.

## Baseline copies (for the git condition)

- `~/repos/sgt-study/baseline-coursecraft` (28 commits)
- `~/repos/sgt-study/baseline-confplan` (26 commits)

These are identical to the sgt copies minus the `.sgt/` directory and minus `Sgt-*` commit
trailers. Same authors, same dates, generated by `make_baseline.sh`. Both test suites pass,
both trees are clean.
