# confplan build log and ground truth

Built 2026-08-09. Repo: `~/repos/sgt-study/confplan`, tag `study-start` at `4241cd0`, 26
commits, 38 tests green. Same episode script as coursecraft (`build-log-coursecraft.md` has the
shapes and subtask semantics); nouns per the isomorphism table in `docs/study/testbed-spec.md`.
Author persona: Sam Park <sam@conf.example.org>.

Key shas:

- E8 tangle (S1): `7ede859` "add talk search" — search command plus silent lowercase-day
  leniency in `slots.parse_slot`.
- Waitlist chain (S2/S3): `f85493e` + `1911a8f` (E11 split across two saves: logic, then CLI
  wiring), `d4051d6` (promotion), `f8de0fd` (cancel cascade), `b4e06f2` (notices); keeper fix
  `de52f62` (back-to-back), tangled keeper `4f9b974` (agenda export + waitlist-hint fix).
- E16 experiment: `dfdcce6` "speaker priority in session queues", reverted cleanly by
  `a8cd83f` (`sgt revert f-02c4a091@3`) — unlike coursecraft, the revert deleted
  `tests/test_priority.py` from disk, so no cleanup commit exists here.
- E17 regression (S4): `704e7a4` introduces `ranges_clash` (boundary `<`), `6ca9a53` routes
  registration through it (the E17 work is split across two saves; the second is the one that
  changes behavior). Leftover `speaker` plumbing in models/cli is the realistic crumb.

Blemishes: E0 dated the build day; E11 and E17 are each split into two same-day saves (edit
mishaps during the build, kept because a real history has such pairs); plan records for E2/E11
over-claim slightly (resolver defect, fixed mid-build).

Baseline twins: `~/repos/sgt-study/baseline-coursecraft` (28 commits) and
`~/repos/sgt-study/baseline-confplan` (26 commits) — identical trees minus `.sgt/`, identical
messages minus `Sgt-*` trailers, same authors and dates, generated by
`make_baseline.sh`. Both suites green, trees clean.
