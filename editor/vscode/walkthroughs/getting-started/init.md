## Initialize semi-git

Run this once per repo, inside an existing git repository. It reads your git history into the
op store under `.sgt/` — nothing about your git history or working tree is changed.

<button data-command="sgt.init">Run sgt init</button>

Equivalent to running `sgt init` in a terminal at the repo root.
