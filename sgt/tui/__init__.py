"""The semi-git terminal UI (Textual). Optional — install with ``semi-git[tui]``.

Kept behind an extra and lazily imported (``sgt tui``) so the core ``sgt`` install stays
dependency-light. The UI is a thin shell over the same ``sgt.api`` projection the CLI and the
VSCode extension consume.
"""
