"""The feature lens (plan Phase P3, U12/U13): op-footprint clustering, hierarchical tree, pins,
and LLM labeling on top of the operation-ideal kernel, plus `map.build_map` (the build-and-save
orchestration) and `verbs` (the feature-verb surface: merge/split/rename/move/revert).
"""
