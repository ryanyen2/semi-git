"""The feature lens (plan Phase P3, U12): op-footprint clustering, hierarchical tree, pins, and
LLM labeling on top of the operation-ideal kernel. Pure engine + persistence -- `sgt map` and the
feature verbs are U13's job, wired against this package's output shape.
"""
